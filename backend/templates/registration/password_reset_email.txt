{% autoescape off %}{% comment %}
  Treść tekstowa listu z linkiem do resetu hasła.

  ``autoescape off`` jest tu poprawnością, a nie obejściem: to jest plik tekstowy, nie HTML, więc
  encje (``&amp;`` zamiast ``&``) byłyby po prostu widoczne w wiadomości. Wersja HTML listu
  (``password_reset_email_body.html``) autoescapowania **nie** wyłącza.

  Poza adresem odbiorcy (który i tak jest w nagłówku ``To:``) w liście nie ma danych osobowych:
  ani imienia, ani szkoły, ani roli. Skrzynka pocztowa bywa cudza.

  Zmienne pochodzą z ``PasswordResetForm.save``; ``protocol`` jest wyliczane z ``request.is_secure()``,
  więc za Caddy (``SECURE_PROXY_SSL_HEADER``) link jest ``https``.
{% endcomment %}Ktoś – prawdopodobnie Ty – poprosił o ustawienie nowego hasła do konta {{ email }} w serwisie {{ site_name }}.

Aby ustawić nowe hasło, otwórz poniższy adres:

{{ protocol }}://{{ domain }}{% url 'web:password-reset-confirm' uidb64=uid token=token %}

Link jest ważny 24 godziny i można go użyć tylko raz. Po zmianie hasła przestaje działać.

Jeśli to nie Ty prosiłeś o reset – zignoruj tę wiadomość. Hasło do konta pozostaje bez zmian
i nikt nie uzyskał do niego dostępu.

--
{{ site_name }}
Wiadomość wysłana automatycznie; prosimy na nią nie odpowiadać.
{% endautoescape %}
